# Управление параллелизмом в UCXSync

## Проблема (до исправления)

В исходной реализации каждая задача синхронизации (комбинация node+share) создавала **собственный семафор** с лимитом `max_parallelism`.

### Пример проблемы:

```
Конфигурация:
- 14 узлов (WU01-WU13 + CU)
- 2 shares на узел (E$, F$)
- max_parallelism = 8

Максимальное количество задач = 14 × 2 = 28

Реальный параллелизм = 28 задач × 8 операций = 224 одновременных операции! ⚠️
```

**Последствия:**
- Перегрузка системы (CPU, память, диск I/O)
- Исчерпание файловых дескрипторов
- Снижение производительности из-за context switching
- Потенциальные ошибки сети из-за большого количества соединений

## Решение

Реализован **глобальный семафор**, который ограничивает общее количество одновременных файловых операций **для всех задач вместе**.

### Архитектура после исправления:

```go
type Service struct {
    // ...
    maxParallelism  int           // Глобальный лимит
    globalSemaphore chan struct{} // Глобальный семафор
    // ...
}
```

### Как это работает:

1. **При старте синхронизации:**
   ```go
   s.globalSemaphore = make(chan struct{}, maxParallelism)
   ```
   Создается один семафор на всё приложение.

2. **При копировании файла:**
   ```go
   s.globalSemaphore <- struct{}{}  // Захват слота
   defer func() { <-s.globalSemaphore }()  // Освобождение
   ```
   Все задачи конкурируют за общий пул слотов.

3. **При остановке:**
   ```go
   s.globalSemaphore = nil  // Освобождение ресурсов
   ```

## Преимущества

### 1. Предсказуемая нагрузка
```
max_parallelism = 8
→ Гарантированно не более 8 одновременных копирований
→ Независимо от количества активных задач
```

### 2. Эффективное использование ресурсов
- Автоматическое распределение слотов между задачами
- Активные задачи получают больше ресурсов
- Неактивные задачи не резервируют слоты

### 3. Защита системы
- Предотвращение перегрузки диска
- Контроль сетевых соединений
- Стабильная работа при большом количестве источников

## Мониторинг

### Веб-интерфейс

Добавлена карточка в Status Panel:
```
┌─────────────────────────────┐
│ Активных копирований        │
│     5 / 8                   │
└─────────────────────────────┘
```

**Цветовая индикация:**
- 🟢 Зеленый: загрузка < 70%
- 🟡 Желтый: загрузка 70-90%
- 🔴 Красный: загрузка > 90%

### API Response

```json
{
  "is_running": true,
  "max_parallelism": 8,
  "active_file_operations": 5,
  "active_tasks": [...]
}
```

## Настройка

### Конфигурация (config.yaml)

```yaml
sync:
  max_parallelism: 8  # Общий лимит для всех задач
```

### Рекомендации по выбору значения:

**Для HDD (механические диски):**
```yaml
max_parallelism: 4  # Низкий параллелизм из-за seek time
```

**Для SSD:**
```yaml
max_parallelism: 8-16  # Средний параллелизм
```

**Для NVMe или RAID:**
```yaml
max_parallelism: 16-32  # Высокий параллелизм
```

**Факторы для учёта:**
- Скорость диска назначения
- Пропускная способность сети
- Количество CPU ядер
- Доступная память

### Формула оценки:

```
max_parallelism = min(
    CPU_cores,
    network_bandwidth_mbps / avg_file_size_mbps,
    disk_iops / 10
)
```

## Тестирование

### Проверка лимита:

1. Установите низкий лимит:
   ```yaml
   max_parallelism: 2
   ```

2. Запустите синхронизацию большого проекта

3. Наблюдайте в веб-интерфейсе:
   ```
   Активных копирований: 2 / 2  (🔴 красный = 100%)
   ```

4. Убедитесь, что значение никогда не превышает 2

### Benchmark:

```bash
# Низкий параллелизм
max_parallelism: 4
→ Время: 15 минут
→ CPU: 20%
→ Диск: 50 MB/s

# Средний параллелизм
max_parallelism: 8
→ Время: 8 минут
→ CPU: 35%
→ Диск: 120 MB/s

# Высокий параллелизм (может быть контрпродуктивным!)
max_parallelism: 32
→ Время: 10 минут (медленнее!)
→ CPU: 80%
→ Диск: 90 MB/s (узкое место)
```

## Технические детали

### Семафор как канал

Go channel используется как семафор:
```go
sem := make(chan struct{}, maxParallelism)

// Захват
sem <- struct{}{}  // Блокируется если канал полон

// Освобождение
<-sem  // Освобождает один слот
```

### Атомарность

Текущее использование семафора считывается атомарно:
```go
activeOps := len(s.globalSemaphore)
```

### Thread-safety

- Семафор создается под мьютексом
- Доступ к семафору thread-safe (канал)
- Освобождение через `defer` (гарантия даже при panic)

## Миграция с предыдущей версии

### Изменения в коде:

**Было:**
```go
// Локальный семафор в каждой задаче
sem := make(chan struct{}, s.maxParallelism)
```

**Стало:**
```go
// Глобальный семафор из Service
s.globalSemaphore <- struct{}{}
defer func() { <-s.globalSemaphore }()
```

### Изменения в API:

**Новые поля в SyncStatus:**
```go
type SyncStatus struct {
    MaxParallelism       int  // Конфигурированный лимит
    ActiveFileOperations int  // Текущее использование
    // ...
}
```

### Обратная совместимость:

✅ Файлы конфигурации не изменились
✅ Старые значения `max_parallelism` работают как раньше
✅ API расширен, но старые поля сохранены

## FAQ

### Q: Что если max_parallelism = 1?
**A:** Последовательное копирование по одному файлу. Медленно, но безопасно.

### Q: Что если max_parallelism слишком большое?
**A:** Система может стать медленнее из-за context switching и конкуренции за диск.

### Q: Можно ли изменить max_parallelism во время работы?
**A:** Нет, требуется остановка и перезапуск синхронизации.

### Q: Влияет ли количество узлов на max_parallelism?
**A:** Нет, лимит глобальный независимо от количества источников.

### Q: Как увеличить производительность?
**A:** 
1. Увеличьте `max_parallelism` (но не слишком)
2. Используйте быстрый диск назначения (SSD/NVMe)
3. Улучшите сетевое соединение (10 Gbps)
4. Добавьте больше RAM для кэширования

## Changelog

### v1.1.0 (2025-11-11)
- ✨ Добавлен глобальный семафор для контроля параллелизма
- ✨ Добавлена метрика `active_file_operations` в API
- ✨ Добавлен UI индикатор активных копирований с цветовой кодировкой
- 🐛 Исправлена проблема с неконтролируемым ростом параллельных операций
- 📝 Добавлена документация по параллелизму

### v1.0.0 (2025-11-07)
- 🎉 Первый релиз
- ⚠️ Проблема: неограниченный параллелизм (до 224 операций)
